<?xml version='1.0'?>
<project name="marauroa" default="compile" basedir=".">
  <property name="version" value="0.1"/>
  <property name="src" value="."/>
  <property name="build" value="build"/>
  <property name="lib"  value="lib"/>
  <property name="classpath"  value="classes;D:\j2sdk1.4.2\junit\junit3.8.1\junit.jar"/>
  <property name="jarname" value="marauroa.jar"/>
  <property name="docs" value="docs"/>
  <property name="runclass" value="marauroa.marauroad"/>


  <target name="init">
    <mkdir dir="${build}" />
    <mkdir dir="${lib}" />
    <mkdir dir="${docs}" />
  </target>

  <target name="compile" depends="init">
    <!-- run javac to compile the source files -->
    <javac srcdir="${src}" destdir="${build}">
      <classpath>
        <pathelement path="${classpath}"/>
      </classpath>
    </javac>
  </target>

  <target name="jar" depends="compile">
    <jar jarfile="${lib}/${jarname}" basedir="${build}/"/>
  </target>
  
  <target name="docs" depends="compile">
    <javadoc packagenames="marauroa.*"
           sourcepath="${src}"
           defaultexcludes="yes"
           destdir="${docs}"
           author="true"
           version="true"
           use="true"
           windowtitle="Marauroa API Documentation Version: ${version}">
      <fileset dir="${src}">
          <include name="**/*.java"/>
      </fileset>
    </javadoc>
  </target>
  
  <target name="run" depends="jar">
    <java classname="${runclass}">
      <classpath>
        <pathelement path="${classpath}"/>
        <fileset dir="${lib}">
            <include name="**/*.jar"/>
        </fileset>
      </classpath>
    </java>
  </target>
  
  <target name="clean">
    <delete dir="${build}"/>
    <delete dir="${lib}"/>
    <delete dir="${docs}"/>
  </target>
</project>